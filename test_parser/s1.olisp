(define ((int -> int) fibonacci)
  (lambda (int -> int) (n)
    (if (or (= n 0) (= n 1))
      1
      (+ (fibonacci (- n 1)) (fibonacci (- n 2))))))

(define (int result) (fibonacci 5)) // result is now the 5th fibonacci number

(print result)

(print "Hello world, \"testers\"!\n")

// Illegal symbol error
(define ((int => int) fibonacci)
  (lambda (int => int) (n)
    (if (or (= n 0) (= n 1))
      1
      (+ (fibonacci (- n 1)) (fibonacci (- n 2))))))

// Unbalanced parentheses
(define (double num) 10.0

// Too many return values
(define ((int int -> int int) myFunction)
  (lambda (int int -> int int) (x y)
    (if (x > y)
      x
      y)))
