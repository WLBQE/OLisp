(define ((int -> int) fibonacci)
  (lambda (int -> int) (n)
    (if (or (= n 0) (= n 1))
      1
      (+ (fibonacci (- n 1)) (fibonacci (- n 2))))))

(define (int result) (fibonacci (d2i 5.25)))
(print result)
