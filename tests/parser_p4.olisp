(class circle
  (member (double radius))
  (member (string color))
  (member ((void -> double) getArea)
    (lambda (void -> double)()
      (* radius radius 3.14)))
  (constructor radius color))

(define (circle c) (circle 1 "Black"))

(print circle.color)
